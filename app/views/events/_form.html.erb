<%= f.collection_select(:category_id, Category.all, :id, :name) %>

<% if @event.errors.any? %>
      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= SAVE errors %></li>
      <% end %>
      </ul>
<% end %>